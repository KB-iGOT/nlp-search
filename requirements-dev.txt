# Dev / test dependencies
# UNVERIFIED versions — no lock file found in this repo.
# Versions below are latest stable as of 2026-06-12.
# Pin exact versions after the first successful CI run.
pytest==8.3.5
pytest-asyncio==0.25.3
pytest-cov==6.1.0
anyio[trio]==4.8.0
# httpx is already in requirements.txt (==0.28.1); listed here so dev
# installs remain self-contained.
httpx==0.28.1
